# Matriz de cobertura

Ver saída `--cov=lib/ml` em `docs/testes/RELATORIO_DE_TESTES.md` e, quando gerado, `artifacts/metrics/coverage.json`.

Módulos críticos: `schema`, `dataset`, `baseline`, `predict`, `llm_contract`, `risco_ml` (fora de `lib/ml`).
