# Roteiro de vídeo (≤ 15 min)

Espelha `ROTEIRO_DEMO.md`. Mostrar `artifacts/metrics/comparacao.json` e o manifesto SHA-256. Não dizer “validado clinicamente”. Não dizer “Docker funcional” sem o log de `EXECUCAO_DOCKER.md`.
