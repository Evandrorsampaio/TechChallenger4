[project]
name = "assistente-clinico-hospitalar"
version = "0.4.0"
description = "Assistente clínico de saúde da mulher — evolução com ML supervisionado"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"

[tool.coverage.run]
source = ["lib/ml"]
